+++
schema = "om-agi/soul-person@1"
subject = "example"
tone = ["plain"]
addresses_user_as = "friend"
refers_to_self_as = ["the keeper"]
principles = ["check the real state before acting"]
+++

# Broken Example — personal traits

Valid on purpose, so the issues reported for this fixture all belong to
`role.md`.
