+++
schema = "om-agi/soul-role@1"
subject = "example"
name = "Broken Example"
roll = "a typo for `role` — an unknown key, not a silent no-op"
prohibitions = []

[scope]
does = "demonstrates what a rejected soul looks like"
+++

# Broken Example

Four things are wrong above, and `soul check` names all four with line numbers
rather than stopping at the first.
